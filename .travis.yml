language: python
python:
  - "2.7"
# command to install dependencies
install:
  - sudo pip install -r requirements.txt
# command to run tests
script:
  - echo "Hello World"
  - echo ${TEST}
  - echo ${TRAVIS_TAG}
